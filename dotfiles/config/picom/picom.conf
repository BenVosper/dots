backend = "glx";
vsync = true;

shadow = true;
shadow-radius = 7;
shadow-opacity = 0.3;

blur:
{
  method = "gaussian";
  size = 7;
  deviation = 5.0;
};

blur-background-exclude = [
  "(! class_g = 'Alacritty')",
];

shadow-exclude = [
  "( class_g = 'Polybar')"
];

fading = true;
fade-delta = 3;
